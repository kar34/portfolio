---
phase: 05-linkedin-video-project-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - portfolio/src/pages/project/linkedin-video.astro
autonomous: true
requirements:
  - REQ-013
  - REQ-016
  - REQ-017

must_haves:
  truths:
    - "Navigating to /project/linkedin-video renders the full case study page, not a stub"
    - "Page title 'LinkedIn Video' appears as a large heading near the top of the page"
    - "Hero image (Videohero.png) renders full-width below the title"
    - "Overview section heading and both paragraphs of body copy appear verbatim"
    - "tabcompressed.gif renders centered between Overview and Impact sections"
    - "Impact section heading and both paragraphs appear verbatim, including hyperlinked TechCrunch, Axios, and TechRadar"
    - "Two mockup images (video mock left.png, video mock right.png) appear side-by-side in a rounded card at the bottom"
    - "'← More Projects' link appears at the very bottom, centered, and links to /work"
    - "Layout matches LinkedInVideoProjectPageDesktop.jpeg on desktop and LinkedInVideoProjectPageMobile.jpeg on mobile"

  artifacts:
    - path: "portfolio/src/pages/project/linkedin-video.astro"
      provides: "Full LinkedIn Video case study page"
      min_lines: 60

  key_links:
    - from: "portfolio/src/pages/project/linkedin-video.astro"
      to: "portfolio/src/layouts/Layout.astro"
      via: "import Layout, slot pattern"
      pattern: "import Layout"
    - from: "'← More Projects' link"
      to: "/work"
      via: "href attribute"
      pattern: "href=\"/work\""
---

<objective>
Implement the /project/linkedin-video case study page, replacing the existing stub with the full pixel-perfect implementation matching LinkedInVideoProjectPageDesktop.jpeg and LinkedInVideoProjectPageMobile.jpeg.

Purpose: Deliver the LinkedIn Video project page — the first of three case study pages in the portfolio.
Output: `portfolio/src/pages/project/linkedin-video.astro` fully implemented with title, hero, overview, GIF, impact, mockups, and back-link.
</objective>

<execution_context>
@/Users/kraymond/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kraymond/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Source of truth for layout and spacing:
- Mocks/LinkedInVideoProjectPageDesktop.jpeg
- Mocks/LinkedInVideoProjectPageMobile.jpeg

If any visual detail is ambiguous in the mocks, WebFetch https://www.kenny-raymond.com/project/linkedin-video for exact values.

<interfaces>
<!-- Layout.astro signature — executor uses this directly -->
From portfolio/src/layouts/Layout.astro:
```astro
interface Props {
  title?: string;
}
// Usage: <Layout title="Page Title">...slot content...</Layout>
// Layout provides: fixed nav, footer, Lenis scroll, hamburger menu
// Body background: black (#000), text: white (#fff), font: Poppins
```

Key Tailwind facts (Tailwind v4, no config file):
- No tailwind.config.js — customization is via CSS @theme blocks in global.css
- Standard Tailwind utility classes work as expected
- Color scheme: black background, white text only (monochromatic)
- Font: Poppins loaded via Google Fonts in Layout.astro head

Assets already in portfolio/public/ — reference with leading slash:
- /Videohero.png
- /tabcompressed.gif
- /video mock left.png
- /video mock right.png
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the full LinkedIn Video project page</name>

  <read_first>
    - portfolio/src/pages/project/linkedin-video.astro (current stub — read to see what to replace)
    - portfolio/src/layouts/Layout.astro (layout interface and class patterns)
    - portfolio/src/components/WorkCards.astro (reference for Tailwind class conventions used in this project)
    - Mocks/LinkedInVideoProjectPageDesktop.jpeg (desktop source of truth — study layout, spacing, typography scale)
    - Mocks/LinkedInVideoProjectPageMobile.jpeg (mobile source of truth — study single-column stack, image sizing)
  </read_first>

  <files>portfolio/src/pages/project/linkedin-video.astro</files>

  <action>
Replace the entire contents of `portfolio/src/pages/project/linkedin-video.astro` with a complete implementation. Do not preserve any stub content.

**Overall page structure** (top to bottom, matching desktop mock):
1. Page title block
2. Hero image (full-width)
3. Overview section
4. GIF (centered, phone-sized)
5. Impact section
6. Two-mockup card (side by side)
7. "← More Projects" back-link

---

**1. Page title block**

Sits below the fixed nav. Use top padding to clear the nav (the nav is ~70px tall — use `pt-20 md:pt-24` or similar). Left-aligned. No category label above the title (the mock shows the title directly).

```html
<section class="px-6 md:px-16 lg:px-24 pt-20 md:pt-24 pb-10 md:pb-14">
  <h1 class="text-4xl md:text-6xl lg:text-7xl font-semibold text-white leading-tight">
    LinkedIn Video
  </h1>
</section>
```

---

**2. Hero image**

Full-width, no horizontal padding. Sits immediately below the title block. Use `w-full h-auto` so it scales responsively.

```html
<section class="w-full">
  <img
    src="/Videohero.png"
    alt="LinkedIn Video hero"
    class="w-full h-auto"
  />
</section>
```

---

**3. Overview section**

Appears below the hero with generous vertical padding. Contains:
- Bold "Overview" heading (not all-caps — sentence case as shown in mock)
- Two paragraphs of body copy (normal weight, readable line-height)

Exact copy (use verbatim — do not paraphrase):

Paragraph 1:
"Video creation and consumption on LinkedIn was growing rapidly year over year, creating a major opportunity for the company. We launched a dedicated video tab that gained traction and quickly became one of LinkedIn's fastest-growing surfaces."

Paragraph 2:
"I led the end-to-end design, shaping the vision, defining the experience, and driving fast execution while maintaining a high quality bar."

```html
<section class="px-6 md:px-16 lg:px-24 py-14 md:py-20">
  <h2 class="text-2xl md:text-3xl font-semibold text-white mb-6">Overview</h2>
  <p class="text-white text-base md:text-lg leading-relaxed mb-6 max-w-3xl">
    Video creation and consumption on LinkedIn was growing rapidly year over year, creating a major opportunity for the company. We launched a dedicated video tab that gained traction and quickly became one of LinkedIn's fastest-growing surfaces.
  </p>
  <p class="text-white text-base md:text-lg leading-relaxed max-w-3xl">
    I led the end-to-end design, shaping the vision, defining the experience, and driving fast execution while maintaining a high quality bar.
  </p>
</section>
```

---

**4. Animated GIF (tabcompressed.gif)**

Centered horizontally. Displayed at phone/mobile screen size — approximately 300–360px wide on desktop (matches the mock where it looks like a phone screen). No background. Significant vertical whitespace above and below.

```html
<section class="flex justify-center px-6 py-10 md:py-16">
  <img
    src="/tabcompressed.gif"
    alt="LinkedIn Video tab interface"
    class="w-72 md:w-80 h-auto"
  />
</section>
```

---

**5. Impact section**

Same padding pattern as Overview. Contains:
- Bold "Impact" heading (sentence case)
- Two paragraphs of body copy
- Second paragraph includes three hyperlinks: TechCrunch, Axios, and TechRadar. Style these links with `underline` so they visually match the live site.

Exact copy:

Paragraph 1:
"Since launching the new video experience, watch time has increased significantly and major creators from other platforms have started sharing video on LinkedIn."

Paragraph 2:
"The success of the team's work elevated the initiative into a top company priority and strategic investment for LinkedIn. The work also received external recognition, with coverage in TechCrunch, Axios, and TechRadar."

Links:
- TechCrunch → https://techcrunch.com (use actual URL if known, otherwise use `#` as placeholder — do not invent wrong URLs; use `#` if unsure)
- Axios → https://axios.com (same caveat)
- TechRadar → https://www.techradar.com (same caveat)

```html
<section class="px-6 md:px-16 lg:px-24 py-14 md:py-20">
  <h2 class="text-2xl md:text-3xl font-semibold text-white mb-6">Impact</h2>
  <p class="text-white text-base md:text-lg leading-relaxed mb-6 max-w-3xl">
    Since launching the new video experience, watch time has increased significantly and major creators from other platforms have started sharing video on LinkedIn.
  </p>
  <p class="text-white text-base md:text-lg leading-relaxed max-w-3xl">
    The success of the team's work elevated the initiative into a top company priority and strategic investment for LinkedIn. The work also received external recognition, with coverage in <a href="https://techcrunch.com" target="_blank" rel="noopener" class="underline">TechCrunch</a>, <a href="https://axios.com" target="_blank" rel="noopener" class="underline">Axios</a>, and <a href="https://www.techradar.com" target="_blank" rel="noopener" class="underline">TechRadar</a>.
  </p>
</section>
```

---

**6. Two mockup images side by side**

Looking at the desktop mock: the two phone mockup images sit inside a light gray rounded card that spans most of the page width. The card has a light gray/off-white background (`bg-neutral-200` or `bg-[#e5e5e5]`), rounded corners (`rounded-2xl` or `rounded-3xl`), and contains both images side by side with gap between them. On mobile, the two images stack vertically (each one full-width within the card).

```html
<section class="px-6 md:px-16 lg:px-24 py-10 md:py-14">
  <div class="bg-neutral-200 rounded-3xl p-8 md:p-12 flex flex-col md:flex-row gap-6 md:gap-8 items-center justify-center">
    <img
      src="/video mock left.png"
      alt="LinkedIn Video mockup — feed view"
      class="w-full md:w-1/2 h-auto"
    />
    <img
      src="/video mock right.png"
      alt="LinkedIn Video mockup — profile view"
      class="w-full md:w-1/2 h-auto"
    />
  </div>
</section>
```

---

**7. "← More Projects" back-link**

Centered at the bottom of the page. Links to `/work`. Matches the mock exactly — arrow + "More Projects" text, centered, white.

```html
<section class="flex justify-center py-16 md:py-24">
  <a href="/work" class="text-white text-base md:text-lg font-medium">
    ← More Projects
  </a>
</section>
```

---

**Full assembled file** — wrap all sections in Layout:

```astro
---
import Layout from '../../layouts/Layout.astro';
---
<Layout title="LinkedIn Video - Kenny Raymond">

  <!-- Title -->
  <section class="px-6 md:px-16 lg:px-24 pt-20 md:pt-24 pb-10 md:pb-14">
    <h1 class="text-4xl md:text-6xl lg:text-7xl font-semibold text-white leading-tight">
      LinkedIn Video
    </h1>
  </section>

  <!-- Hero image -->
  <section class="w-full">
    <img
      src="/Videohero.png"
      alt="LinkedIn Video hero"
      class="w-full h-auto"
    />
  </section>

  <!-- Overview -->
  <section class="px-6 md:px-16 lg:px-24 py-14 md:py-20">
    <h2 class="text-2xl md:text-3xl font-semibold text-white mb-6">Overview</h2>
    <p class="text-white text-base md:text-lg leading-relaxed mb-6 max-w-3xl">
      Video creation and consumption on LinkedIn was growing rapidly year over year, creating a major opportunity for the company. We launched a dedicated video tab that gained traction and quickly became one of LinkedIn's fastest-growing surfaces.
    </p>
    <p class="text-white text-base md:text-lg leading-relaxed max-w-3xl">
      I led the end-to-end design, shaping the vision, defining the experience, and driving fast execution while maintaining a high quality bar.
    </p>
  </section>

  <!-- GIF -->
  <section class="flex justify-center px-6 py-10 md:py-16">
    <img
      src="/tabcompressed.gif"
      alt="LinkedIn Video tab interface"
      class="w-72 md:w-80 h-auto"
    />
  </section>

  <!-- Impact -->
  <section class="px-6 md:px-16 lg:px-24 py-14 md:py-20">
    <h2 class="text-2xl md:text-3xl font-semibold text-white mb-6">Impact</h2>
    <p class="text-white text-base md:text-lg leading-relaxed mb-6 max-w-3xl">
      Since launching the new video experience, watch time has increased significantly and major creators from other platforms have started sharing video on LinkedIn.
    </p>
    <p class="text-white text-base md:text-lg leading-relaxed max-w-3xl">
      The success of the team's work elevated the initiative into a top company priority and strategic investment for LinkedIn. The work also received external recognition, with coverage in <a href="https://techcrunch.com" target="_blank" rel="noopener" class="underline">TechCrunch</a>, <a href="https://axios.com" target="_blank" rel="noopener" class="underline">Axios</a>, and <a href="https://www.techradar.com" target="_blank" rel="noopener" class="underline">TechRadar</a>.
    </p>
  </section>

  <!-- Mockup images -->
  <section class="px-6 md:px-16 lg:px-24 py-10 md:py-14">
    <div class="bg-neutral-200 rounded-3xl p-8 md:p-12 flex flex-col md:flex-row gap-6 md:gap-8 items-center justify-center">
      <img
        src="/video mock left.png"
        alt="LinkedIn Video mockup — feed view"
        class="w-full md:w-1/2 h-auto"
      />
      <img
        src="/video mock right.png"
        alt="LinkedIn Video mockup — profile view"
        class="w-full md:w-1/2 h-auto"
      />
    </div>
  </section>

  <!-- Back link -->
  <section class="flex justify-center py-16 md:py-24">
    <a href="/work" class="text-white text-base md:text-lg font-medium">
      ← More Projects
    </a>
  </section>

</Layout>
```

**Visual calibration note:** After writing the file, WebFetch https://www.kenny-raymond.com/project/linkedin-video and compare heading sizes, spacing, and body copy size against the live site. If anything looks off, adjust Tailwind classes to match. The live site is the final arbiter — not the plan text.
  </action>

  <verify>
    <automated>grep -n "Videohero.png" /Users/kraymond/Library/CloudStorage/OneDrive-Microsoft/Desktop/Other/NewPortfolioSite/portfolio/src/pages/project/linkedin-video.astro && grep -n "tabcompressed.gif" /Users/kraymond/Library/CloudStorage/OneDrive-Microsoft/Desktop/Other/NewPortfolioSite/portfolio/src/pages/project/linkedin-video.astro && grep -n "video mock left.png" /Users/kraymond/Library/CloudStorage/OneDrive-Microsoft/Desktop/Other/NewPortfolioSite/portfolio/src/pages/project/linkedin-video.astro && grep -n "video mock right.png" /Users/kraymond/Library/CloudStorage/OneDrive-Microsoft/Desktop/Other/NewPortfolioSite/portfolio/src/pages/project/linkedin-video.astro && grep -n 'href="/work"' /Users/kraymond/Library/CloudStorage/OneDrive-Microsoft/Desktop/Other/NewPortfolioSite/portfolio/src/pages/project/linkedin-video.astro && grep -n "TechCrunch" /Users/kraymond/Library/CloudStorage/OneDrive-Microsoft/Desktop/Other/NewPortfolioSite/portfolio/src/pages/project/linkedin-video.astro && grep -n "import Layout" /Users/kraymond/Library/CloudStorage/OneDrive-Microsoft/Desktop/Other/NewPortfolioSite/portfolio/src/pages/project/linkedin-video.astro</automated>
  </verify>

  <acceptance_criteria>
    - File contains `import Layout from '../../layouts/Layout.astro'`
    - File contains `src="/Videohero.png"` (hero image referenced)
    - File contains `src="/tabcompressed.gif"` (GIF referenced)
    - File contains `src="/video mock left.png"` (left mockup referenced)
    - File contains `src="/video mock right.png"` (right mockup referenced)
    - File contains `href="/work"` (More Projects link points to /work)
    - File contains the text `← More Projects`
    - File contains `Overview` as a heading (h2 element)
    - File contains `Impact` as a heading (h2 element)
    - File contains `TechCrunch` with an `<a` tag wrapping it
    - File contains `Axios` with an `<a` tag wrapping it
    - File contains `TechRadar` with an `<a` tag wrapping it
    - File contains `bg-neutral-200` (the mockup card background)
    - File contains `rounded-3xl` or `rounded-2xl` (the mockup card rounding)
    - File contains `flex-col md:flex-row` or equivalent responsive stacking for the mockup pair
    - File is NOT the old stub (does not contain `Coming soon`)
    - File line count is >= 60 lines
  </acceptance_criteria>

  <done>
    The page at /project/linkedin-video renders the full LinkedIn Video case study — title heading, full-width hero, Overview section with two paragraphs of exact copy, the tabcompressed.gif centered at phone scale, Impact section with two paragraphs and three underlined external links, two phone mockups side by side in a rounded gray card, and a centered "← More Projects" link at the bottom. The layout matches the desktop mock on wide screens and stacks to a single column on mobile. No animations, no hover effects. The stub "Coming soon" text is gone.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — /project/linkedin-video</name>

  <read_first>
    - Mocks/LinkedInVideoProjectPageDesktop.jpeg (compare side-by-side with browser)
    - Mocks/LinkedInVideoProjectPageMobile.jpeg (compare at mobile viewport)
  </read_first>

  <what-built>Complete LinkedIn Video project page at /project/linkedin-video — title, hero, overview, GIF, impact, mockups, back-link.</what-built>

  <how-to-verify>
    1. Start the dev server if not already running: `cd /Users/kraymond/Library/CloudStorage/OneDrive-Microsoft/Desktop/Other/NewPortfolioSite/portfolio && npm run dev`
    2. Open http://localhost:4321/project/linkedin-video in your browser
    3. Compare against Mocks/LinkedInVideoProjectPageDesktop.jpeg:
       - Title "LinkedIn Video" is large and bold at the top
       - Videohero.png renders full-width immediately below the title
       - "Overview" heading appears, followed by two paragraphs of copy
       - tabcompressed.gif appears centered at phone size between Overview and Impact
       - "Impact" heading appears, followed by two paragraphs — TechCrunch, Axios, and TechRadar are underlined links
       - Two phone mockup images appear side by side inside a light gray rounded card
       - "← More Projects" centered at the very bottom, links to /work
    4. Resize browser to mobile width (~390px) and compare against LinkedInVideoProjectPageMobile.jpeg:
       - Title stays left-aligned and slightly smaller
       - Hero image is full-width
       - The two mockup images stack vertically (one above the other) within the gray card
       - All copy is readable at mobile font sizes
    5. Click "← More Projects" — confirm it navigates to /work
  </how-to-verify>

  <resume-signal>Type "approved" if the page matches the mocks. Describe any visual discrepancies if issues are found (e.g., "GIF is too large", "mockup card background is wrong color", "heading font size is off").</resume-signal>
</task>

</tasks>

<verification>
- `grep -c "Coming soon" portfolio/src/pages/project/linkedin-video.astro` returns 0 (stub is gone)
- `grep -c "Videohero.png" portfolio/src/pages/project/linkedin-video.astro` returns >= 1
- `grep -c "tabcompressed.gif" portfolio/src/pages/project/linkedin-video.astro` returns >= 1
- `grep -c "video mock left.png" portfolio/src/pages/project/linkedin-video.astro` returns >= 1
- `grep -c "video mock right.png" portfolio/src/pages/project/linkedin-video.astro` returns >= 1
- `grep -c 'href="/work"' portfolio/src/pages/project/linkedin-video.astro` returns >= 1
- Dev server compiles with no errors: `cd portfolio && npm run build` exits 0
</verification>

<success_criteria>
- /project/linkedin-video renders the full case study (not the stub)
- All 4 assets render (Videohero.png, tabcompressed.gif, video mock left.png, video mock right.png)
- Exact copy matches the live site verbatim for both Overview and Impact sections
- TechCrunch, Axios, and TechRadar are hyperlinked with underline styling
- Page passes human visual verification against desktop and mobile mocks
- "← More Projects" navigates to /work
</success_criteria>

<output>
After completion, create `.planning/phases/05-linkedin-video-project-page/05-01-SUMMARY.md` covering:
- What was built
- Any visual calibration adjustments made (class changes from plan defaults)
- Verification result
- Any open issues
</output>
