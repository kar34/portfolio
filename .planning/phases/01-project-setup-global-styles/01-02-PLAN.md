---
phase: 01-project-setup-global-styles
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - portfolio/src/layouts/Layout.astro
  - portfolio/src/pages/index.astro
autonomous: true
requirements:
  - REQ-003
  - REQ-004
  - REQ-006
must_haves:
  truths:
    - "Every page shows the KR logo top-left and a hamburger menu icon top-right"
    - "Every page shows the footer with Kenny Raymond name and 3 social links (LinkedIn, Email, Soundcloud) with arrow icons"
    - "Smooth scrolling is active on all pages via Lenis"
    - "The favicon appears in the browser tab"
    - "Dev server starts without errors on npm run dev"
  artifacts:
    - path: "portfolio/src/layouts/Layout.astro"
      provides: "Base layout with head, nav, footer, Lenis init, favicon"
      min_lines: 60
      contains: "Layout"
    - path: "portfolio/src/pages/index.astro"
      provides: "Placeholder home page using Layout"
      contains: "Layout"
  key_links:
    - from: "portfolio/src/layouts/Layout.astro"
      to: "portfolio/src/styles/global.css"
      via: "CSS import in frontmatter"
      pattern: "import.*global.css"
    - from: "portfolio/src/layouts/Layout.astro"
      to: "lenis"
      via: "script tag initializing Lenis"
      pattern: "new Lenis"
    - from: "portfolio/src/pages/index.astro"
      to: "portfolio/src/layouts/Layout.astro"
      via: "Layout component import"
      pattern: "import Layout"
---

<objective>
Build the base Layout.astro component with navigation (KR logo + hamburger menu), footer (name + 3 social links with arrow icons), Lenis smooth scroll initialization, and favicon. Create a placeholder index.astro to verify the full stack works.

Purpose: Establish the shared layout shell that every page will use, ensuring consistent nav, footer, and smooth scroll across the site.
Output: Layout.astro with nav + footer + Lenis, index.astro placeholder, working dev server.
</objective>

<execution_context>
@/Users/kraymond/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kraymond/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-project-setup-global-styles/01-01-SUMMARY.md

Reference the Mocks for nav and footer design:
- Nav (visible on ALL mocks): KR circle logo (white circle with "KR" text inside, outlined) positioned top-left. Hamburger menu icon (three horizontal lines / "=" style) positioned top-right. This is consistent on BOTH desktop and mobile.
- Footer (visible on HomeMobile.jpeg and AboutMobile.jpeg): "Kenny Raymond" heading, then three links stacked vertically: "LinkedIn" with arrow, "Email" with arrow, "Soundcloud" with arrow. The arrow is the "Footer Social Arrow.png" asset. Footer has a subtle top border (dark gray line separating it from content).

NOTE ON NAV DESIGN: CLAUDE.md states "Logo left, links (Work, About) right — collapses to hamburger on mobile." However, ALL 10 Mocks/ screenshots (both desktop and mobile — HomeDesktop.jpeg, HomeMobile.jpeg, AboutDesktop.jpeg, AboutMobile.jpeg, and all 6 project page mocks) consistently show a hamburger/toggle icon on the right side at ALL breakpoints, with no visible inline text links. Since the Mocks are the source of truth per CLAUDE.md's own guidance ("Read these before writing any code. They are the source of truth for layout, spacing, and visual design."), the hamburger-only navigation on all breakpoints is the correct implementation. The CLAUDE.md text description is overridden by the visual evidence in the mocks.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Layout.astro with nav, footer, favicon, and global CSS import</name>
  <files>portfolio/src/layouts/Layout.astro</files>
  <read_first>
    portfolio/src/styles/global.css (confirm it exists from Plan 01),
    portfolio/astro.config.mjs (confirm Tailwind v4 via @tailwindcss/vite),
    portfolio/package.json (confirm lenis is installed),
    Mocks/HomeMobile.jpeg (footer structure reference),
    Mocks/HomeDesktop.jpeg (nav structure reference),
    Mocks/AboutMobile.jpeg (footer structure reference)
  </read_first>
  <action>
Create `portfolio/src/layouts/Layout.astro` with the following structure:

**Astro frontmatter section (between the component script delimiters):**

    interface Props {
      title?: string;
    }

    const { title = 'Kenny Raymond - Product Designer' } = Astro.props;
    import '../styles/global.css';

(Wrap the above in Astro's component script delimiters at the top of the file.)

**HTML template (after the component script):**

The file should render an html element with lang="en" containing head and body.

HEAD contents:
- meta charset="UTF-8"
- meta name="viewport" content="width=device-width, initial-scale=1.0"
- link rel="icon" type="image/png" href="/favicon.png"
- title element using the {title} prop

BODY contents (in order):

1. NAV element
2. MAIN element containing a slot for page content
3. FOOTER element
4. Two script tags (Lenis + menu toggle)

**Nav implementation details (from mocks — hamburger on ALL breakpoints, confirmed by all 10 mock screenshots):**
- Fixed/sticky positioning at the top of the page
- Full width, with padding (approximately px-6 py-4 on mobile, px-8 py-5 on desktop)
- Left side: KR logo. This is a white-outlined circle with "KR" text inside. This is NOT an image file. It is rendered as an inline SVG. The circle is approximately 40px diameter, thin white border (1-2px), with "KR" in white text centered inside. Use an inline SVG with: width="40" height="40" viewBox="0 0 40 40", a circle element at cx="20" cy="20" r="19" stroke="white" stroke-width="1.5" fill="none", and a text element at x="20" y="24" text-anchor="middle" fill="white" font-family="Poppins, sans-serif" font-size="14" font-weight="500" containing "KR". Wrap in an anchor tag linking to "/" with aria-label="Home".
- Right side: Hamburger menu icon. Three horizontal lines as an SVG. Approximately 28px wide, 20px tall. Use a button with id="menu-toggle" and aria-label="Toggle menu", class="relative z-50". Inside, an SVG with width="28" height="20" viewBox="0 0 28 20", containing three line elements: (x1=0 y1=1 x2=28 y2=1), (x1=0 y1=10 x2=28 y2=10), (x1=0 y1=19 x2=28 y2=19), all with stroke="white" stroke-width="2".
- Menu overlay: a div with id="menu-overlay", classes "fixed inset-0 bg-black z-40 flex flex-col items-center justify-center gap-8 hidden". Contains three anchor links: Home (/), Work (/work), About (/about), each with classes "text-white text-3xl font-medium".
- Nav container styling: "fixed top-0 left-0 right-0 z-50 flex items-center justify-between px-6 py-5 md:px-10"

**Footer implementation details (from HomeMobile.jpeg, AboutMobile.jpeg):**
- Top border: subtle dark gray line using "border-t border-white/10" or "border-neutral-800"
- Padding: "py-12 px-6 md:px-10"
- "Kenny Raymond" text: large heading, approximately text-2xl or text-3xl, font-medium, mb-6
- Below that, three social links stacked vertically with spacing (space-y-2 or space-y-3):
  - "LinkedIn" followed by arrow icon
  - "Email" followed by arrow icon
  - "Soundcloud" followed by arrow icon
- Each link is a flex row with: text on left, arrow icon image on right
- Arrow icon: use "/Footer Social Arrow.png" as img src. It is a small diagonal arrow (upper-right pointing), approximately 12-16px. Use classes "w-3 h-3".
- Link destinations:
  - LinkedIn: href="https://www.linkedin.com/in/kennyraymond/" target="_blank" rel="noopener noreferrer"
  - Email: href="mailto:kennyraymond20@gmail.com"
  - Soundcloud: href="https://soundcloud.com/dj-m3lodic" target="_blank" rel="noopener noreferrer"
- Each link has classes: "flex items-center gap-1 text-base text-white/80"

**Lenis smooth scroll — use a standard `<script>` tag (NOT `<script is:inline>`):**
The `<script>` tag (without is:inline) allows Vite to bundle the ESM import. Inside the script tag:
Import Lenis from 'lenis'. Also import 'lenis/dist/lenis.css' for default styles. Create new Lenis instance with options: lerp: 0.15, wheelMultiplier: 1. Create a raf function that calls lenis.raf(time) and requestAnimationFrame(raf). Start with requestAnimationFrame(raf).

**Menu toggle — use a separate `<script>` tag (NOT `<script is:inline>`):**
Get menu-toggle button and menu-overlay div by ID. Add click listener to toggle button that toggles the "hidden" class on overlay. Add click listeners to all anchor links inside overlay that add "hidden" class back (close menu on navigation).

IMPORTANT: Do NOT add any CSS animations or hover transitions. Per CLAUDE.md: "Animations are deferred."
  </action>
  <verify>
    <automated>cd /Users/kraymond/Library/CloudStorage/OneDrive-Microsoft/Desktop/Other/NewPortfolioSite/portfolio && test -f src/layouts/Layout.astro && grep -q "favicon.png" src/layouts/Layout.astro && grep -q "global.css" src/layouts/Layout.astro && grep -q "Lenis" src/layouts/Layout.astro && grep -q "Kenny Raymond" src/layouts/Layout.astro && grep -q "linkedin.com/in/kennyraymond" src/layouts/Layout.astro && grep -q "kennyraymond20@gmail.com" src/layouts/Layout.astro && grep -q "soundcloud.com/dj-m3lodic" src/layouts/Layout.astro && grep -q "Footer Social Arrow" src/layouts/Layout.astro && grep -q "menu-toggle" src/layouts/Layout.astro && grep -q "slot" src/layouts/Layout.astro && grep -q "noopener" src/layouts/Layout.astro && echo "LAYOUT_VALID"</automated>
  </verify>
  <acceptance_criteria>
    - portfolio/src/layouts/Layout.astro exists and is at least 60 lines
    - Contains link rel="icon" type="image/png" href="/favicon.png" in head
    - Contains import of '../styles/global.css' in component script
    - Contains nav element with KR logo SVG (circle + "KR" text)
    - Contains hamburger menu button with id="menu-toggle"
    - Contains menu overlay div with id="menu-overlay" and links to /, /work, /about
    - Contains footer with "Kenny Raymond" text
    - Contains three social links: linkedin.com/in/kennyraymond, mailto:kennyraymond20@gmail.com, soundcloud.com/dj-m3lodic
    - Each external link has target="_blank" and rel="noopener noreferrer"
    - Contains Footer Social Arrow.png image references
    - Contains slot element for page content injection
    - Lenis script uses `<script>` (NOT `<script is:inline>`) so Vite can bundle the ESM import
    - Contains Lenis initialization script with "new Lenis" and "import Lenis from 'lenis'"
    - Menu toggle script uses `<script>` (NOT `<script is:inline>`)
    - NO CSS animations or transitions present (no @keyframes, no transition-*, no animate-*)
  </acceptance_criteria>
  <done>Layout.astro created with nav (KR logo + hamburger menu on all breakpoints), footer (Kenny Raymond + 3 social links with arrow icons), Lenis smooth scroll, and favicon configured</done>
</task>

<task type="auto">
  <name>Task 2: Create placeholder index.astro and verify dev server</name>
  <files>portfolio/src/pages/index.astro</files>
  <read_first>
    portfolio/src/layouts/Layout.astro (confirm Layout component exists and its Props interface)
  </read_first>
  <action>
1. Create `portfolio/src/pages/index.astro` as a placeholder page that uses the Layout component.

The file should have a component script section that imports Layout from '../layouts/Layout.astro'.

The template should wrap content in the Layout component with title="Kenny Raymond - Product Designer". Inside the Layout, place a section with classes "min-h-screen flex items-center justify-center" containing an h1 with classes "text-4xl font-semibold" and text "Kenny Raymond".

This is a temporary placeholder. The full home page will be built in Phase 2. The purpose here is to verify:
- The Layout renders correctly (nav + footer visible)
- Global styles are applied (black bg, white text, Poppins font)
- Tailwind CSS classes work
- Lenis smooth scroll is active
- Favicon appears in browser tab

2. Start the dev server to verify everything works:

Run `npm run dev` from the portfolio/ directory.

Check the terminal output for:
- No errors
- Server starts on localhost (typically http://localhost:4321)
- No Tailwind CSS compilation errors
- No missing module errors

If the dev server shows errors:
- Missing module: check package.json and run npm install
- Tailwind errors: verify astro.config.mjs has @tailwindcss/vite plugin configured
- Lenis errors: verify import path in Layout.astro script tag

3. After confirming the dev server starts, stop it (Ctrl+C). The verification step will run `npm run build` as a deterministic check.
  </action>
  <verify>
    <automated>cd /Users/kraymond/Library/CloudStorage/OneDrive-Microsoft/Desktop/Other/NewPortfolioSite/portfolio && grep -q "import Layout" src/pages/index.astro && npm run build 2>&1 | tail -5 && echo "BUILD_SUCCESS"</automated>
  </verify>
  <acceptance_criteria>
    - portfolio/src/pages/index.astro exists
    - Contains import of Layout from '../layouts/Layout.astro'
    - Contains Layout component wrapper
    - Contains "Kenny Raymond" text
    - Uses Tailwind classes (text-4xl, font-semibold, etc.)
    - `npm run build` completes without errors (exit code 0)
    - Build output directory (dist/) is created with index.html
    - Built index.html contains: Poppins font reference, black background style, favicon link, "Kenny Raymond" text
  </acceptance_criteria>
  <done>Placeholder index.astro created, dev server runs without errors, build completes successfully. Full Phase 1 foundation is working.</done>
</task>

</tasks>

<verification>
1. `npm run build` in portfolio/ completes without errors
2. Built HTML in dist/index.html contains:
   - Poppins font import
   - favicon.png link
   - Nav with KR logo SVG
   - Footer with "Kenny Raymond" and social links
   - Lenis initialization script
3. `grep -r "animation\|transition\|@keyframes" portfolio/src/` returns no matches (no animations)
4. All external links have rel="noopener noreferrer"
</verification>

<success_criteria>
- Layout.astro is a complete shell: head (favicon, meta, title), nav (KR logo + hamburger + menu overlay), main (slot), footer (name + 3 social links with arrows), Lenis script
- index.astro uses the Layout and renders a placeholder
- `npm run build` produces a working static site with no errors
- Global styles visible: black background, white text, Poppins font
- No CSS animations or transitions anywhere in src/
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-global-styles/01-02-SUMMARY.md`
</output>
