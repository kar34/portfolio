---
phase: 02-home-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - portfolio/src/pages/index.astro
autonomous: true
requirements:
  - REQ-008
  - REQ-009
  - REQ-016
  - REQ-017
must_haves:
  truths:
    - "User sees KENNY RAYMOND in large uppercase text at top of page"
    - "User sees circular portrait photo centered in hero"
    - "User sees kennyraymond20@gmail.com as clickable email link in hero"
    - "User sees LinkedIn, Email, and SoundCloud social icons in hero"
    - "User sees PRODUCT DESIGNER in large uppercase text below portrait row"
    - "User sees Based in Los Angeles below the title"
    - "User sees ABOUT heading and bio snippet text below hero"
    - "Layout is centered single-column on mobile and properly laid out on desktop"
  artifacts:
    - path: "portfolio/src/pages/index.astro"
      provides: "Home page hero section and about section"
      contains: "KENNY RAYMOND"
  key_links:
    - from: "portfolio/src/pages/index.astro"
      to: "portrait image in public folder"
      via: "img src attribute for circular portrait"
      pattern: "ken sm"
    - from: "portfolio/src/pages/index.astro"
      to: "mailto:kennyraymond20@gmail.com"
      via: "anchor href for email link"
      pattern: "mailto:kennyraymond20"
    - from: "portfolio/src/pages/index.astro"
      to: "portfolio/src/layouts/Layout.astro"
      via: "Astro layout import"
      pattern: "import Layout from"
---

<objective>
Build the Home page hero section (name, portrait, email, social icons, title, location) and About section (heading + bio snippet) in index.astro, replacing the placeholder content from Phase 01.

Purpose: These are the first two visual sections of the home page. Getting the hero pixel-perfect establishes the visual language for the entire site.
Output: portfolio/src/pages/index.astro with hero and about sections matching HomeDesktop.jpeg and HomeMobile.jpeg.
</objective>

<execution_context>
@/Users/kraymond/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kraymond/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-global-styles/01-02-SUMMARY.md

<interfaces>
Layout.astro interface (executor imports this and wraps page content):
- Props: { title?: string } with default "Kenny Raymond - Product Designer"
- Imports global.css
- Usage: wrap page content in Layout tag with optional title prop

global.css:
- Tailwind v4 with Poppins font, black bg (#000), white text (#fff)
- @theme { font-poppins: Poppins, sans-serif }
- Base: html uses Poppins, body min-h-screen, a color inherit no underline, img max-w-full

Available assets in portfolio/public/:
- b&w ken sm.png -- circular portrait for hero
- LinkedIn Logo Icon (1).png -- LinkedIn icon (white, small)
- Email Logo Icon (1).png -- Email icon (white, small)
- SoundCloud Logo Icon (1).png -- SoundCloud icon (white, small)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build hero section with name, portrait, email, icons, title, and location</name>
  <files>portfolio/src/pages/index.astro</files>

  <read_first>
    - Mocks/HomeDesktop.jpeg (source of truth for desktop layout)
    - Mocks/HomeMobile.jpeg (source of truth for mobile layout)
    - portfolio/src/layouts/Layout.astro (layout wrapper to import)
    - portfolio/src/styles/global.css (available Tailwind theme tokens)
    - CLAUDE.md (project constraints -- no animations, black/white only, Poppins)
  </read_first>

  <action>
Replace the entire content of portfolio/src/pages/index.astro with the hero section implementation.

The file MUST start with an Astro frontmatter block importing Layout from '../layouts/Layout.astro', then wrap all content in a Layout tag (no title prop needed, default is correct).

**Layout structure from mocks analysis:**

The hero section is the first section on the page, occupying roughly the viewport height. It is centered horizontally with generous top padding to clear the fixed nav.

**Desktop layout (HomeDesktop.jpeg):**
- "KENNY RAYMOND" is a large uppercase heading, centered, near the top
- Below the name: a row containing the email link on the left, circular portrait in the center, and social icons on the right
- Below that row: "PRODUCT DESIGNER" in large uppercase, centered
- Below that: "Based in Los Angeles" in smaller text, right-aligned within the content area

**Mobile layout (HomeMobile.jpeg):**
- "KENNY RAYMOND" centered, large uppercase, broken into two lines
- Circular portrait below, centered
- "PRODUCT DESIGNER" below portrait, centered, broken into two lines
- "Based in Los Angeles" centered below title
- Social icons (LinkedIn, Email, SoundCloud) displayed as a row below "Based in Los Angeles", centered

**Exact copy text:**
- Name: `KENNY RAYMOND` (all caps via CSS uppercase class)
- Email: `kennyraymond20@gmail.com` (lowercase, displayed as text, links to mailto:)
- Title: `PRODUCT DESIGNER` (all caps via CSS uppercase class)
- Location: `Based in Los Angeles`

**Hero section structure (inside Layout tag):**

SECTION element with classes: pt-28 pb-20 px-6 md:px-10
  DIV container with classes: max-w-4xl mx-auto

    H1 (name) with classes: text-4xl md:text-5xl lg:text-6xl font-semibold uppercase tracking-tight text-center
      Text: Kenny Raymond

    Portrait row container: DIV with classes flex flex-col items-center mt-6 md:mt-8
      Inner row: DIV with classes flex items-center justify-center gap-6 md:gap-10 w-full

        Email link (desktop only): A href="mailto:kennyraymond20@gmail.com" with classes hidden md:block text-sm text-white/80
          Text: kennyraymond20@gmail.com

        Portrait: DIV with classes w-24 h-24 md:w-28 md:h-28 rounded-full overflow-hidden flex-shrink-0
          IMG src="/b&w ken sm.png" alt="Kenny Raymond portrait" with classes w-full h-full object-cover

        Social icons (desktop only): DIV with classes hidden md:flex items-center gap-3
          A href="https://www.linkedin.com/in/kennyraymond/" target="_blank" rel="noopener noreferrer" aria-label="LinkedIn"
            IMG src="/LinkedIn Logo Icon (1).png" alt="LinkedIn" classes w-5 h-5
          A href="mailto:kennyraymond20@gmail.com" aria-label="Email"
            IMG src="/Email Logo Icon (1).png" alt="Email" classes w-5 h-5
          A href="https://soundcloud.com/dj-m3lodic" target="_blank" rel="noopener noreferrer" aria-label="SoundCloud"
            IMG src="/SoundCloud Logo Icon (1).png" alt="SoundCloud" classes w-5 h-5

    H2 (title) with classes: text-3xl md:text-4xl lg:text-5xl font-semibold uppercase tracking-tight text-center mt-4 md:mt-6
      Text: Product Designer

    P (location) with classes: text-sm text-white/60 text-center md:text-right mt-2
      Text: Based in Los Angeles

    Mobile-only social icons: DIV with classes flex md:hidden items-center justify-center gap-4 mt-4
      (Same 3 icon links as desktop version above)

  End container DIV
End SECTION

Then add a placeholder comment: WORK section will be added in plan 02

End Layout tag.

**Key constraints:**
- NO CSS animations or transitions anywhere (per CLAUDE.md)
- NO hover effects
- Use only black (#000 bg from body) and white (#fff text) with opacity variants for subdued text
- Poppins font is already set globally -- no need to specify font-family
- The portrait image MUST be circular (rounded-full + overflow-hidden)
- The email link in the hero is a mailto: link
- Social icon images use the exact filenames from public/ including spaces and parentheses
- The entire section must be wrapped in the Layout component imported from ../layouts/Layout.astro
- On mobile, the email text is NOT shown next to portrait -- it only appears on desktop. On mobile, only the social icons show below the location text.
  </action>

  <verify>
    <automated>cd /Users/kraymond/Library/CloudStorage/OneDrive-Microsoft/Desktop/Other/NewPortfolioSite/portfolio && PATH=~/.volta/tools/image/node/24.6.0/bin:$PATH npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - portfolio/src/pages/index.astro exists and imports Layout from '../layouts/Layout.astro'
    - File contains "Kenny Raymond" in an h1 tag with classes including "uppercase"
    - File contains img src="/b&w ken sm.png" with "rounded-full" on parent or element
    - File contains href="mailto:kennyraymond20@gmail.com" (at least twice -- email link + icon link)
    - File contains img src="/LinkedIn Logo Icon (1).png"
    - File contains img src="/Email Logo Icon (1).png"
    - File contains img src="/SoundCloud Logo Icon (1).png"
    - File contains "Product Designer" in an h2 with "uppercase" class
    - File contains "Based in Los Angeles"
    - File contains "md:hidden" class on mobile-only social icon row
    - File contains "hidden md:flex" or "hidden md:block" for desktop-only elements
    - npm run build exits with code 0
    - File does NOT contain "transition", "animate-", "@keyframes", or "hover:"
  </acceptance_criteria>

  <done>Hero section renders with name heading, circular portrait, email link, 3 social icons, PRODUCT DESIGNER title, and Based in Los Angeles. Desktop shows email left of portrait and icons right of portrait. Mobile shows icons centered below location. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add About section below hero</name>
  <files>portfolio/src/pages/index.astro</files>

  <read_first>
    - Mocks/HomeDesktop.jpeg (about section layout -- heading left, text right)
    - Mocks/HomeMobile.jpeg (about section layout -- everything centered/left-aligned in single column)
    - portfolio/src/pages/index.astro (current state after Task 1)
  </read_first>

  <action>
Add the About section to index.astro, placed after the hero section and before the closing Layout tag.

**Desktop layout (HomeDesktop.jpeg):**
- "ABOUT" is a large uppercase heading, left-aligned
- Bio text is positioned to the right side of the content area, in a narrower column
- There is significant vertical spacing between the hero and the about section

**Mobile layout (HomeMobile.jpeg):**
- "ABOUT" heading is large, left-aligned
- Bio text is below the heading, left-aligned, full width

**Exact copy text:**
- Heading: `ABOUT` (all caps via CSS uppercase class)
- Bio: `With 10+ years of experience, I shape product experiences through compelling storytelling and a bias toward building.`

**About section structure (insert after hero section, before the WORK placeholder comment):**

SECTION with classes: py-20 md:py-28 px-6 md:px-10
  DIV container with classes: max-w-4xl mx-auto
    DIV flex container with classes: flex flex-col md:flex-row md:justify-between md:items-start gap-8 md:gap-16

      H2 (heading) with classes: text-4xl md:text-5xl font-semibold uppercase tracking-tight
        Text: About

      P (bio) with classes: text-base md:text-lg leading-relaxed text-white/90 md:max-w-sm md:text-right
        Text: With 10+ years of experience, I shape product experiences through compelling storytelling and a bias toward building.

    End flex DIV
  End container DIV
End SECTION

Update the placeholder comment to say: WORK section will be added in plan 02

**Key constraints:**
- The heading "ABOUT" must use the CSS `uppercase` class -- write "About" in the HTML
- Bio text should be right-aligned on desktop (md:text-right), left-aligned on mobile (default)
- Bio text should be constrained to a narrow column on desktop (md:max-w-sm)
- The flex layout switches from column (mobile) to row (desktop) with md:flex-row
- NO animations, transitions, or hover effects
  </action>

  <verify>
    <automated>cd /Users/kraymond/Library/CloudStorage/OneDrive-Microsoft/Desktop/Other/NewPortfolioSite/portfolio && PATH=~/.volta/tools/image/node/24.6.0/bin:$PATH npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - portfolio/src/pages/index.astro contains an h2 with text "About" and class "uppercase"
    - File contains the exact string "With 10+ years of experience, I shape product experiences through compelling storytelling and a bias toward building."
    - The about section has "md:flex-row" for desktop side-by-side layout
    - The bio paragraph has "md:text-right" and "md:max-w-sm" classes
    - The about section appears AFTER the hero section in the file
    - npm run build exits with code 0
    - File does NOT contain "transition", "animate-", "@keyframes", or "hover:"
  </acceptance_criteria>

  <done>About section renders below hero with "ABOUT" heading left-aligned and bio text right-aligned on desktop. On mobile, heading and bio stack vertically. Build passes.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` succeeds (exit code 0)
2. The built dist/index.html contains all hero content (name, portrait, email, icons, title, location) and about content (heading, bio)
3. No CSS animations or transitions in the output
4. Portfolio layout (nav + footer) wraps the content via Layout component
</verification>

<success_criteria>
- index.astro renders hero section with KENNY RAYMOND, circular portrait, email link, 3 social icons, PRODUCT DESIGNER, Based in Los Angeles
- index.astro renders about section with ABOUT heading and bio snippet
- Desktop layout: email left of portrait, icons right of portrait, about heading left / bio right
- Mobile layout: everything centered/stacked, icons below location, about section single column
- Static build completes without errors
- Zero CSS animations or transitions
</success_criteria>

<output>
After completion, create `.planning/phases/02-home-page/02-01-SUMMARY.md`
</output>
